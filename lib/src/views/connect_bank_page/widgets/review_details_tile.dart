import 'package:flutter/material.dart';

class ReviewDetailsTile extends StatelessWidget {
  const ReviewDetailsTile({super.key});

  @override
  Widget build(BuildContext context) => const Row(
        children: [],
      );
}
